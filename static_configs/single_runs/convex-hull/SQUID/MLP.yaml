task:
  type: ConvexHullMembershipTask
  dataset: SQUIDBoundaries
  dataset_config:
    normalization: True
  ood_dataset: GaussianBlobs
  ood_config:
    length: 300
    min_points: 200
    max_points: 300
    centers: 4
    dim: 2
backbone:
  type: SetNN
  d_model: 16
  embedding_layers: 3
  embedding_hidden_dim: 64
batch_size: 32
learning_rate: 1.e-4
loss_function: scatter_binary_focal_loss