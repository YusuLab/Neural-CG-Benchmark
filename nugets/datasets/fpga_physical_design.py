"""FPGA physical-design datasets.

The local FPGA corpora are cataloged, but their on-disk sample schema is not
yet normalized enough for a stable first-class loader. This module exists so the
domain taxonomy is represented in ``nugets.datasets`` while active loaders are
added incrementally.
"""
